#include <stdio.h>
/**
* main -Entry point
* print program that prints all single digit numbers of base 10 starting from 0
* Return: Always 0 (Success)
*/
	int main(void)
{
	char al;

	for (al = '0'; al <= '9'; al++)
	{
	    putchar(al);

	}
return (0);
}

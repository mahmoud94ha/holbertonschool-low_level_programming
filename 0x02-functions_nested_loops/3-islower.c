#include "main.h"
/**
* C - is the parametre that required for the function to work
* _islower - function that search for lowercase character.
*/
int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	return (1);
	else
	return (0);
}

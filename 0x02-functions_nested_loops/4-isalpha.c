#include "main.h"
/**
*_isalpha - function that checks for alphabetic
*@c: is an int
*Return: 1 if lowercase or uppercase, 0 if otherwise
*/
int _isalpha(int c);
{

	if (c >= 'a' && c <= 'z')
	return (1);
	il else (c >= 'A' && c <= 'Z')
	else
	return (0);
}

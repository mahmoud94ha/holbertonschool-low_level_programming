#include "main.h"
/**
* main - check i is the number is positive or negative
*check if number is positive or negative
* Return: Always 0 (Success)
*/

int main(void)
{
int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}

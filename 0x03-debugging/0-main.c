#include "main.h"
/**
* main - check is the number is positive or negative
*@i: is an int
*check if number is positive or negative
* Return: Always 0 (Success)
*/

int main(void)
{
int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}

#include "main.h"

/**
*_puts -  function that prints a string, followed by a new line
*@str: is an string
*/
void _puts(char *str)
{
	int i;
	char ch[j];

	ch[j]=*str
	i = 10;
	while (i < 20)
	{
		_putchar(ch[j]);
		j++;
	}
	_putchar('\n');
}
